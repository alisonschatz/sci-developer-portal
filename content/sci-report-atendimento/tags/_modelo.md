---
tag: NomeExatoDaTag
# Configurações opcionais de override:
# renameTo: Novo Nome Exibido    # Altera o nome exibido da tag e atualiza as operações associadas
# hide: true                     # Oculta a tag e todas as suas operações na documentação
---

Descreva aqui o grupo de operações pertencente a esta tag. O conteúdo do corpo é renderizado como a descrição da tag em Markdown.

### Orientações

1. **Nome da Tag (`tag`):**
   Deve corresponder **exatamente** ao nome da tag declarado na especificação OpenAPI do backend.

2. **Uso do `renameTo`:**
   Ao utilizar `renameTo`, mantenha o nome original no campo `tag:`. O renomeamento é aplicado após os demais overrides e alterará a exibição e as âncoras de navegação do grupo.

---
*Nota: Arquivos iniciados com `_` funcionam como modelos/rascunhos e são ignorados no build.*