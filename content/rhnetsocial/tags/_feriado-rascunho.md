---
tag: Feriado
---

RASCUNHO (prefixo `_` = ignorado pelo build). Pendência herdada da v1:
a descrição original do backend diz "criação, edição e exclusão", mas o
recurso é só leitura via API. Quando a escrita definitiva for feita,
renomeie este arquivo (tirando o `_`) e escreva a descrição correta aqui.
